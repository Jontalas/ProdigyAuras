-- ProdigyAuras - Events.lua
-- Handles game events for the addon

ProdigyAuras = ProdigyAuras or {}
-- local addon = ProdigyAuras -- uncomment if needed
-- local ADDON_NAME = "ProdigyAuras" -- uncomment if needed

-- ProdigyAuras:DebugPrint("Events.lua cargado.") -- Use addon:DebugPrint if addon table is set up